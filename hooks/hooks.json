{
  "description": "Produces a trust report on what the agent's \"done\" claim is worth when its turn ends. Report mode by default: it never blocks. Opt into enforcement with \"mode\": \"enforce\" in .greenwash/config.json.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/greenwash_hook.py"],
            "timeout": 300
          }
        ]
      }
    ]
  }
}
